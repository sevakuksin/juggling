# Archive

Earlier experiments and prototypes, kept for reference. **Nothing here is built
or deployed** — the live product is in [`../view-vite/`](../view-vite/).

Safe to delete if no longer needed.

- `web/` — earlier canvas-based web prototype
- `juggling_0.ipynb`, `sample.ipynb` — exploratory notebooks
- `juggling_viz.py`, `rebuild_notebook.py` — notebook/visualization scripts
- `assets/` — original `left_hand.svg` source (view-vite now keeps its own copy)
- `data/`, `models/` — placeholders from earlier work
